[project]
name = "kaggle-store-sales-ts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
    "pytest",
    "scikit-learn",
    "seaborn",
]
